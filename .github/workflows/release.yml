name: MemOS CLI — Release

on:
  # Inspect the trusted result after any internal or fork PR is merged into the
  # official main branch. Fork code never receives release secrets or write
  # permissions; only the resulting push inside this repository may release.
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      version:
        description: "Owner-approved CLI SemVer without leading v. Do not invent a version."
        required: true
      target_ref:
        description: "Git ref to inspect or release. Use main for a real release."
        required: false
        default: "main"
      dry_run:
        description: "Preview evidence and docs only; never create a tag or GitHub Release."
        required: true
        type: boolean
        default: true
      create_draft_release:
        description: "For a real run, create a Draft Release first. Publish it manually to emit release.published."
        required: true
        type: boolean
        default: true
      recover_existing_release:
        description: "Allow an intentional rerun to create a missing GitHub Release for an existing matching tag."
        required: true
        type: boolean
        default: false
      fault_case:
        description: "Dry-run-only changelog quality fault injection."
        required: true
        type: choice
        default: "none"
        options:
          - none
          - mixed_language
          - missing_source_refs
          - invalid_source_ref
          - missing_important_commit
          - thirteen_items
          - too_long
      publish_confirmation:
        description: "Required only when dry_run=false. Must exactly equal: PUBLISH v<version>"
        required: false
        default: ""

concurrency:
  group: memos-cloud-cli-release
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  resolve:
    if: ${{ github.repository == 'MemTensor/MemOS-Cloud-CLI' }}
    runs-on: ubuntu-22.04
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      eligible: ${{ steps.resolve.outputs.eligible || 'false' }}
      reason: ${{ steps.resolve.outputs.reason }}
      version: ${{ steps.resolve.outputs.version }}
      target_ref: ${{ steps.resolve.outputs.target_ref }}
      dry_run: ${{ steps.resolve.outputs.dry_run }}
      create_draft_release: ${{ steps.resolve.outputs.create_draft_release }}
      recover_existing_release: ${{ steps.resolve.outputs.recover_existing_release }}
      fault_case: ${{ steps.resolve.outputs.fault_case }}
      publish_confirmation: ${{ steps.resolve.outputs.publish_confirmation }}
      release_source_mode: ${{ steps.resolve.outputs.release_source_mode }}
      npm_version_exists: ${{ steps.resolve.outputs.npm_version_exists }}
      npm_git_head: ${{ steps.resolve.outputs.npm_git_head }}
      recovery_required: ${{ steps.resolve.outputs.recovery_required }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: >-
            ${{
              github.event_name == 'push' && github.event.after ||
              github.event.repository.default_branch
            }}
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Resolve manual or merged-main version intent
        id: resolve
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
          PUSH_REF: ${{ github.ref }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
          PUSH_AFTER_SHA: ${{ github.event.after }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_TARGET_REF: ${{ inputs.target_ref || 'main' }}
          INPUT_DRY_RUN: ${{ inputs.dry_run }}
          INPUT_CREATE_DRAFT_RELEASE: ${{ inputs.create_draft_release }}
          INPUT_RECOVER_EXISTING_RELEASE: ${{ inputs.recover_existing_release }}
          INPUT_FAULT_CASE: ${{ inputs.fault_case || 'none' }}
          INPUT_PUBLISH_CONFIRMATION: ${{ inputs.publish_confirmation }}
        run: node .github/scripts/resolve-cli-release.mjs

  prepare:
    if: ${{ needs.resolve.outputs.eligible == 'true' }}
    needs: resolve
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    permissions:
      # GitHub's generate-notes endpoint requires Contents: write even though
      # it only returns a preview. The script remains fail-closed and dry-run
      # still cannot create tags or Releases.
      contents: write
    outputs:
      current_tag: ${{ steps.prepare.outputs.current_tag }}
      previous_tag: ${{ steps.prepare.outputs.previous_tag }}
      target_sha: ${{ steps.prepare.outputs.target_sha }}
      release_notes_file: ${{ steps.prepare.outputs.release_notes_file }}
      inspection_dir: ${{ steps.prepare.outputs.inspection_dir }}
      docs_action: ${{ steps.prepare.outputs.docs_action }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Fetch branches and release tags
        shell: bash
        run: |
          set -euo pipefail
          git fetch --tags --force origin
          git fetch origin '+refs/heads/*:refs/remotes/origin/*'

      - name: Test release evidence and quality guards
        run: node --test .github/scripts/*.test.mjs

      - name: Prepare Release and Plugin tab inspection
        id: prepare
        env:
          RELEASE_VERSION: ${{ needs.resolve.outputs.version }}
          TARGET_REF: ${{ needs.resolve.outputs.target_ref }}
          DRY_RUN: ${{ needs.resolve.outputs.dry_run }}
          CREATE_DRAFT_RELEASE: ${{ needs.resolve.outputs.create_draft_release }}
          PUBLISH_CONFIRMATION: ${{ needs.resolve.outputs.publish_confirmation }}
          RELEASE_FAULT_CASE: ${{ needs.resolve.outputs.fault_case }}
          RELEASE_SOURCE_MODE: ${{ needs.resolve.outputs.release_source_mode }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          GITHUB_TOKEN: ${{ github.token }}
          DOC_AGENT_RELEASE_NOTES_DRAFT_URL: ${{ secrets.DOC_AGENT_RELEASE_NOTES_DRAFT_URL }}
          DOC_AGENT_RELEASE_NOTES_DRAFT_TOKEN: ${{ secrets.DOC_AGENT_RELEASE_NOTES_DRAFT_TOKEN }}
          DOC_AGENT_RELEASE_FAILURE_URL: ${{ secrets.DOC_AGENT_RELEASE_FAILURE_URL }}
        run: node .github/scripts/prepare-cli-release.mjs

      - name: Upload release inspection
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: memos-cloud-cli-release-inspection
          path: ${{ runner.temp }}/memos-cloud-cli-release-inspection
          if-no-files-found: error
          retention-days: 14

      - name: Summarize inspection
        if: ${{ always() }}
        shell: bash
        env:
          PREPARE_OUTCOME: ${{ steps.prepare.outcome }}
          DRY_RUN: ${{ needs.resolve.outputs.dry_run }}
          FAULT_CASE: ${{ needs.resolve.outputs.fault_case }}
          RELEASE_SOURCE_MODE: ${{ needs.resolve.outputs.release_source_mode }}
          CURRENT_TAG: ${{ steps.prepare.outputs.current_tag }}
          PREVIOUS_TAG: ${{ steps.prepare.outputs.previous_tag }}
          TARGET_SHA: ${{ steps.prepare.outputs.target_sha }}
          DOCS_ACTION: ${{ steps.prepare.outputs.docs_action }}
        run: |
          {
            echo "## MemOS CLI release inspection"
            echo
            echo "- prepare_outcome: ${PREPARE_OUTCOME}"
            echo "- dry_run: ${DRY_RUN}"
            echo "- fault_case: ${FAULT_CASE}"
            echo "- release_source_mode: ${RELEASE_SOURCE_MODE}"
            echo "- range: ${PREVIOUS_TAG}...${CURRENT_TAG}"
            echo "- target: ${TARGET_SHA}"
            echo "- evidence_scope: whole_repository"
            echo "- docs_action: ${DOCS_ACTION}"
            echo
            echo "The artifact contains README.md, github-release-notes.md/release-notes.md, release-notes-source.json, evidence.json, release-notes-draft.json, docs-preview.md/json, quality-report.json, and release-contract.json."
            if [ "${PREPARE_OUTCOME}" != "success" ]; then
              echo
              echo "Preparation failed closed. Review the redacted quality-report.json in the uploaded artifact."
            fi
            if [ "${DRY_RUN}" = "true" ]; then
              echo
              echo "No tag, GitHub Release, Docs PR, or deployment was created."
            fi
          } >> "${GITHUB_STEP_SUMMARY}"

  build:
    if: >-
      ${{
        needs.resolve.outputs.eligible == 'true' &&
        needs.resolve.outputs.dry_run != 'true'
      }}
    needs:
      - resolve
      - prepare
    permissions:
      contents: read
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            build_script: ./scripts/build-binary.sh
            upload_name: linux-x64
          - os: windows-2022
            build_script: ./scripts/build-binary.ps1
            upload_name: windows-x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.target_sha }}
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Build archive on Unix
        if: runner.os != 'Windows'
        run: bash ${{ matrix.build_script }}

      - name: Build archive on Windows
        if: runner.os == 'Windows'
        run: pwsh -File ${{ matrix.build_script }}

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: memos-cli-${{ matrix.upload_name }}
          path: dist/*.tar.gz
          if-no-files-found: error
          retention-days: 14

  release:
    if: >-
      ${{
        needs.resolve.outputs.eligible == 'true' &&
        needs.resolve.outputs.dry_run != 'true'
      }}
    needs:
      - resolve
      - prepare
      - build
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.target_sha }}
          fetch-depth: 0
          persist-credentials: true

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Fetch release tags
        shell: bash
        run: git fetch --tags --force origin

      - uses: actions/download-artifact@v4
        with:
          pattern: memos-cli-*
          path: dist
          merge-multiple: true

      - uses: actions/download-artifact@v4
        with:
          name: memos-cloud-cli-release-inspection
          path: release-inspection

      - name: Create or safely resume the tag and GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          CURRENT_TAG: ${{ needs.prepare.outputs.current_tag }}
          TARGET_SHA: ${{ needs.prepare.outputs.target_sha }}
          RECOVER_EXISTING_RELEASE: ${{ needs.resolve.outputs.recover_existing_release }}
          RELEASE_VERSION: ${{ needs.resolve.outputs.version }}
          RELEASE_SOURCE_MODE: ${{ needs.resolve.outputs.release_source_mode }}
          NPM_VERSION_EXISTS: ${{ needs.resolve.outputs.npm_version_exists }}
          NPM_GIT_HEAD: ${{ needs.resolve.outputs.npm_git_head }}
        run: bash .github/scripts/publish-cli-release.sh
